#include <stdlib.h>
#include <stdio.h>
#include <pthread.h>
#include <errno.h>

#define SUDOKU_SIZE 9
typedef stuct {
    int grid[SUDOKU_SIZE][SUDOKU_SIZE];
} sudoku_t;

void sudoku_solve(sudoku_t *s, int nthread) {
    if(n < 1 || n > 9)
        exit(EXIT_FAILURE);
}

int main(void) {
    return(EXIT_SUCCESS);
}
